repositories {
    jcenter()
    flatDir {
        dirs 'aar'
    }
}
dependencies {
    compile fileTree(dir: 'libs', include: '*.aar')
}
android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}