@using PayIn.Web.App.Factories;

@using (Html.xpPopupDelete(
    AccessControlFactory.Delete,
    AccessControlFactory.DeleteApi,
    "",
    "resources.accessControl.delete",
    id: "id"
))
{
	<p translate="resources.accessControl.deleteQuestion" >Do you want to delete this enclosure?</p>
}
