@using PayIn.Web.App.Factories;

@using (Html.xpPopupDelete(
	ExhibitorFactory.Delete,
	ExhibitorFactory.DeleteApi,
	"",
	"resources.exhibitor.delete",
	id: "id"
))
{
	<p translate="resources.exhibitor.deleteQuestion" translate-values="{ name: '{{arguments.name}}',  event: '{{$stateParams.eventName}}'}">Do you want to delete this exhibitor?</p>
}

