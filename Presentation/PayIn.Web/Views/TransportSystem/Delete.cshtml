@using Microsoft.AspNet.Identity
@using PayIn.Common.Resources;
@using PayIn.Web.App.Factories;

@using (Html.xpPopupDelete(
	TransportSystemFactory.Delete,
	TransportSystemFactory.DeleteApi,
	"",
	"resources.transportSystem.delete",
	id: "id"
))
{
	<p translate="resources.transportSystem.deleteQuestion" translate-values="{ name: '{{arguments.name}}' }">Do you want to delete the transport system {{arguments.name}}?</p>
}

