@using PayIn.Web.App.Factories;

@using (Html.xpPopupDelete(
    TranslationFactory.Delete,
    TranslationFactory.DeleteApi,
    "",
    "resources.translation.delete",
    id: "id",
    //init: "scope.arguments.noticeId = params.noticeId; scope.arguments.eventId = params.eventId;",
    //successUrl: "{{history.back()}}"
    success: "window.history.back();"
))
{
    <p translate="resources.translation.deleteQuestion">Do you want to delete this translation?</p>
}

