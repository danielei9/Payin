@using Microsoft.AspNet.Identity
@using PayIn.Common.Resources;
@using PayIn.Web.App.Factories;

@using (Html.xpPopupDelete(
	ServiceGroupFactory.Delete,
	ServiceGroupFactory.DeleteApi,
	"",
	"resources.serviceGroup.delete",
	id: "id"
))
{
<p translate="resources.serviceGroup.deleteQuestion" translate-values="{ name: '{{arguments.name}}' }">Do you want to delete group '{{arguments.name}}' from the category?</p>
}

